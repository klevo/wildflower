$.jlm.bind('wild_posts.wf_edit', function() {
    
    $('#WildPostTitle').focus();
    
    // Edit sections
    $('a[@href=#Categories]').click(function() {
        
        return false;
    });
    
});
